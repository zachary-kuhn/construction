var previousTitle = 'Code Complete';
var newTitle = 'Code Complete, volume 2';

var minReaderCount = 0;
var highestReaderCount = 25;

var isLocked = article.authorId !== author.id;
var isAccessible = !isLocked;